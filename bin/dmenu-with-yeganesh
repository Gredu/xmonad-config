#!/bin/sh
# Script to launch dmenu with colors matching IR_Black theme
# Author: Vic Fryzel
# http://github.com/vicfryzel/xmonad-config


exec $(dmenu_path | /usr/bin/yeganesh -- -fn '-*-terminus-*-r-normal-*-*-120-*-*-*-*-iso8859-*' \
    -nb '#1D1F21' -nf '#c5c8c6' -sb '#7C7C7C' -sf '#CEFFAC')
